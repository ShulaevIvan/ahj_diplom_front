export default class PinnedMessage {
    constructor(appTag) {
        this.appContainer = document.querySelector(appTag);

    }

    addPinned(msgTag) {
        
    }

    removePinned(msgTag) {

    }
}